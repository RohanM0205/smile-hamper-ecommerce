"use client";

import { useRouter } from "next/navigation";
//import ProductFormPage, { type ProductFormPageProps } from "./ProductFormPage";

//export default function ProductFormWrapper({
  //product,
//}: //Pick<ProductFormPageProps, "product">) {
  const router = useRouter();

  //return (
    //<ProductFormPage
      //product={product}
     // onClose={() => router.push("/admin/products")}
    ///>
  //);
//}
